<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/src/main/java/dev/rajivm/mongo/MongoClientProvider.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/main/java/dev/rajivm/mongo/MongoClientProvider.java" />
              <option name="originalContent" value="package dev.rajivm.mongo;&#10;&#10;import com.mongodb.client.MongoClient;&#10;import com.mongodb.client.MongoClients;&#10;import com.mongodb.client.MongoDatabase;&#10;&#10;/**&#10; * Simple provider for a MongoClient. Reads connection string from environment variable MONGODB_URI&#10; * or falls back to mongodb://localhost:27017. Keeps a single shared client.&#10; */&#10;public class MongoClientProvider {&#10;    private static final String DEFAULT_URI = &quot;mongodb://localhost:27017&quot;;&#10;    private static volatile MongoClient client;&#10;&#10;    private MongoClientProvider() {}&#10;&#10;    public static MongoClient getClient() {&#10;        if (client == null) {&#10;            synchronized (MongoClientProvider.class) {&#10;                if (client == null) {&#10;                    String uri = System.getenv(&quot;MONGODB_URI&quot;);&#10;                    if (uri == null || uri.isBlank()) {&#10;                        uri = DEFAULT_URI;&#10;                    }&#10;                    client = MongoClients.create(uri);&#10;                }&#10;            }&#10;        }&#10;        return client;&#10;    }&#10;&#10;    public static MongoDatabase getDatabase(String dbName) {&#10;        return getClient().getDatabase(dbName);&#10;    }&#10;&#10;    public static void close() {&#10;        if (client != null) {&#10;            client.close();&#10;            client = null;&#10;        }&#10;    }&#10;}&#10;" />
              <option name="updatedContent" value="package dev.rajivm.mongo;&#10;&#10;import com.mongodb.client.MongoClient;&#10;import com.mongodb.client.MongoClients;&#10;import com.mongodb.client.MongoDatabase;&#10;&#10;/**&#10; * Simple provider for a MongoClient. Reads connection string from environment variable MONGODB_URI&#10; * or falls back to mongodb://localhost:27017. Keeps a single shared client.&#10; */&#10;public class MongoClientProvider {&#10;    private static final String DEFAULT_URI = &quot;mongodb://localhost:27017&quot;;&#10;    private static volatile MongoClient client;&#10;&#10;    private MongoClientProvider() {}&#10;&#10;    public static MongoClient getClient() {&#10;        if (client == null) {&#10;            synchronized (MongoClientProvider.class) {&#10;                if (client == null) {&#10;                    String uri = System.getenv(&quot;MONGODB_URI&quot;);&#10;                    if (uri == null || uri.isBlank()) {&#10;                        uri = DEFAULT_URI;&#10;                    }&#10;                    client = MongoClients.create(uri);&#10;                }&#10;            }&#10;        }&#10;        return client;&#10;    }&#10;&#10;    public static MongoDatabase getDatabase(String dbName) {&#10;        return getClient().getDatabase(dbName);&#10;    }&#10;&#10;    public static void close() {&#10;        if (client != null) {&#10;            client.close();&#10;            client = null;&#10;        }&#10;    }&#10;}&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>